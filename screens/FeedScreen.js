import React from "react";
import { Text, View } from "react-native";

export default function FeedScreen({ navigation, count, setCount }) {
  return (
    <View>
      <Text style={{ fontSize: 32 }}>FeedScreen</Text>
    </View>
  );
}
